// Re-export MCP SDK types for convenience
export type {
  CallToolResult,
  EmbeddedResource,
  GetPromptResult,
  ImageContent,
  Prompt,
  Resource,
  TextContent,
  Tool,
} from "@modelcontextprotocol/sdk/types.js";
