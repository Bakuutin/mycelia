// Re-export MCP SDK types for convenience
export type {
  Tool,
  CallToolResult,
  TextContent,
  ImageContent,
  EmbeddedResource,
  Resource,
  Prompt,
  GetPromptResult,
} from "npm:@modelcontextprotocol/sdk/types.js";